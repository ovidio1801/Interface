@model List<AcreditacionEmpleado>


@section Styles{
    <link href="~/lib/datatables/css/dataTables.bootstrap4.css" rel="stylesheet" />
}

@{
    ViewData["Title"] = "Acreditación de Beneficios";
    List<DescuentoTarjeta> tars = ViewBag.tars;
}

<h6>Acreditación de Beneficios</h6>
<hr />

<div class="row">
    <div class="text-danger">
        @if (ViewBag.Errores != null)
        {
            <ul>
                @foreach (string item in ViewBag.Errores)
                {
                    <li>@item.ToString()</li>
                }
            </ul>
        }
    </div>
    <div class="text-success">
        @if (ViewBag.Mensaje != null)
        {
            <ul>
                <li>@ViewBag.Mensaje</li>
            </ul>
        }
    </div>
</div>

<form asp-action="LoginCheques" method="post">
    <div class="row">
        <div class="col-sm-12">
            <div class="datatable">
                <table class="table table-bordered table-striped" id="Tabla">
                    <thead>
                        <tr>
                            <th class="text-sm-center">Tipo</th>
                            <th class="text-sm-center">Fecha</th>
                            <th class="text-sm-center">Empleado</th>
                            <th class="text-sm-center">Monto</th>
                            <th class="text-sm-center">Cuenta</th>
                            <th class="text-sm-center">Centro</th>
                        </tr>
                    </thead>
                    <tbody>
                        @if (Model != null)
                        {
                            
                            for (int i = 0; i < Model.Count(); i++)
                            {
                                <tr>
                                    <td class="text-sm-left">@Model[i].ca_tp_pago</td>
                                    <td class="text-sm-left">@Model[i].ca_fec_pago</td>
                                    <td class="text-sm-left">@Model[i].ca_num_empl</td>
                                    <td class="text-sm-right">@Model[i].ca_monto_pg.ToString("C")</td>
                                    <td class="text-sm-left">@Model[i].ca_cta_acre</td>
                                    <td class="text-sm-left">@Model[i].ca_centro</td>
                                </tr>
                                
                            }
                        }
                    </tbody>
                </table>
            </div>
        </div>
    </div>
    @* <br /> *@
    <hr/>
    <div class="row">
        <div class="col-sm-12">
            <div class="form-group">
                <input type="submit" name="Regresar" value="Regresar" class="btn btn-primary float-left" asp-action="Index" />
                <input type="submit" name="LoginPI" value="Procesar Beneficios" class="btn btn-danger float-right" asp-action="LoginCheques" />
            </div>
        </div>
    </div>


</form>


@section Scripts {
    <script src="~/lib/datatables/js/jquery.dataTables.js"></script>
    <script src="~/lib/datatables/js/dataTables.bootstrap4.js"></script>

}



